// Aisla /reset-password del Shell principal para que no muestre la barra lateral
export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
